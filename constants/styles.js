import { StyleSheet } from "react-native";

export const heading = StyleSheet.create({
    sm: {
        fontSize: 16,
        fontWeight: 'bold'
    },
    md: {
        fontSize: 18,
        color: '#DDDDDD',
        fontWeight: 'bold'
    },
    lg: {
        fontSize: 20,
        fontWeight: 'bold'
    }
});